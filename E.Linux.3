#Use the sort command to sort all the lines of a file while removing duplicates
*sort a: sorts file a
*uniq a: shows only the unique entries of file a

#Show only the unique entries of file a.txt and then sort the lines of file a.txt
*uniq; sort a.txt

*uniq a.txt; sort a.txt

*uniq & sort a.txt

*uniq a.txt
*sort a.txt
 
*terminal kept freezing up, could not see results.
